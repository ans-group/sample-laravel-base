# Docker-specific environment variables
# These should match your host user to avoid permission issues

# Set these to your host user's UID and GID
# Find them by running: id -u (for USER_ID) and id -g (for GROUP_ID)
USER_ID=1000
GROUP_ID=1000
